# SafeWorld T26G-FM1 — FINAL Fable 5 Execution Prompt
## Full-L2 Matched Selector Study: train the four frozen arms on the frozen
## FA2 prospective cohort, lock predictions, compute the frozen endpoints and
## apply the ten gates exactly once — EVERYTHING BELOW IS FROZEN BY T26G-FM0

Generated by T26G-FM0 on 2026-08-03T07:31:12.825249+00:00.
FM0 record: /home/qiren/alpasafe/safeworld-alpamayo/artifacts/safeworld_t26g_fm0_full_l2_matched_selector_preregistration/20260803T060900Z

## Execution authorization

T26G-FM0 completed with terminal status:

```text
T26G_FM0_FULL_L2_MATCHED_SELECTOR_PREREGISTERED_READY_FOR_T26G_FM1
```

The user must explicitly authorize T26G-FM1 before anything below runs.
FM1 executes the frozen design and NOTHING else: no architecture, loss,
fold, endpoint, gate, margin or tie-rule may be changed. If any frozen
input fails verification, STOP and finalize with the blocker status.

## 0. Authoritative inputs (verify ALL before any training step)

```text
FA2_REC  /home/qiren/alpasafe/safeworld-alpamayo/artifacts/safeworld_t26g_fa2_prospective_full_l2_acquisition/20260730T190521Z
FA2_PAY  /storage/alpasafe/safeworld-alpamayo/payloads/safeworld_t26g_fa2_prospective_full_l2_acquisition/20260730T190521Z
dataset  results/t26g_fa2_prospective_dataset.json
         sha256 e84ddde8c24991a40935cfc464f7f60755f99ef44a8934db3d95d00715056e96
         173 scenes / 519 groups / 4,152 candidates / K=8
cohort   primary_ids_sha256 c6c94732c5f93ed90fee7deea1c13ff25671fa7aff16a8b1c53dc26849c13fa9
         reserve_ids_sha256 478dfce3f322121e79ba2df31689cc6edaa9c2007bff75f3ef928c5a981a8945
pins     Alpamayo bf580713f08656674827cd6e09888c79cf65fbf2
         AlpaSim  a1f05bb628f3d1d19d79d44188e836e9108f98c6
         evaluator 196d21ab86593af121b055995d0185bb786d1f70
         catalog csv sha256 b1cc2d2fd0235fcfbaac7258a2943e1076376f7f1d51c64553e1a73b5797a7e0
capture  6d43679e4a1a6b68be2317ce4541e1966397f92d8f59f1dc42275619350d013a (alpamayo_base.py, unchanged vs FA1 baseline)
labels   FM0 future-state arrays: /home/qiren/alpasafe/safeworld-alpamayo/artifacts/safeworld_t26g_fm0_full_l2_matched_selector_preregistration/20260803T060900Z/arrays/ (519 npz)
         manifest sha256 575fd7da4790fc4d0f3b1d434ab07b289ed21472f2e59423ee3b422663e0b126
pointer  derived FA2 payload pointer sha256 afef8c2f70378d0276de276b0994918fe9f3f69131217865d30ffec34427427f
         (17,646 registered scientific files, 0 mismatches, all verified)
```

Alpamayo weight files (HF cache, revision-pinned):

```text
    model-00001-of-00005.safetensors  sha256 537259bb56815f9dfdcb028d5606e84dff5ab0c8e0e559782d63d91b6671d15c
    model-00002-of-00005.safetensors  sha256 18841e5049b7836c16c034123ae6d40b2ff0414f9f77008bc7ca72490302a47b
    model-00003-of-00005.safetensors  sha256 e8953b27fe827a60604a006b07ab1216fe766375f9926d1c3bd5ecee84ecef78
    model-00004-of-00005.safetensors  sha256 604f0c0f19986f363a23a1e0b5736eedadd231ca285beab28d5db968a1d37602
    model-00005-of-00005.safetensors  sha256 9d889c09634e5a21b4c957957ede2cdab4418c268ee1d870c1f942d04242adf6
```

## 1. Frozen design (from FM0 contracts — reread, never restate)

```text
    t26g_fm0_architecture_contract.json  sha256 dd7ce3cf5dc84bfe104d16c24a764415096d78e4a6ced280e94d0c122fa75d92
    t26g_fm0_arm_contract.json  sha256 8c4130220cd5d590bc446b28a7ab58c762fca537ef96fd2f01f7d0c0beeabe84
    t26g_fm0_comparison_graph_contract.json  sha256 8a0df1b028c0c4bd7b898f868077d0f1b971051c68a8371d75e5625e71aec026
    t26g_fm0_endpoint_and_statistics_contract.json  sha256 9a7d960780a612d2a66f94a3d36f109ac0b624497d84fa26719d68e884c9d24b
    t26g_fm0_label_mapping_contract.json  sha256 8c2468db55bb312a2a25d9d0b5123323591438feab0649b385045935fea8db57
    t26g_fm0_objective_checkpoint_contract.json  sha256 6cb9161e1c13c896d7eaac1c29ed97a99a339e43db2eb04b869eb4652a8ec39d
    t26g_fm0_prediction_lock_contract.json  sha256 56d2bdf39b92af1d731c313bdb631d92b28df31dbbf7cf4e252df360c3954f62
    t26g_fm0_promotion_gate_contract.json  sha256 296c72ce23c00fae0229cc9f6657b730ded360e7b0dc5123e8591f153f9810df
    t26g_fm0_run_population_contract.json  sha256 6b341582189814aa979cb58e80b124e260ef7dd2471900c71616a80646f14b83
    t26g_fm0_tie_semantics_contract.json  sha256 f1b5e4a1cdb6dfb411dd086d7ebb0441cb19c3fdf9f50ab66f77cc2e3181a746
    t26g_fm0_training_and_compute_contract.json  sha256 cba4a7aa3f78a7025c7393186397550a34b5cb77c415a1b9c6325801556df97a
    t26g_fm0_untouched_confirmation_cohort_contract.json  sha256 4883357d17e18ec80c1332853d4e0ac6be877f8b16dfd5a7691f407813efa978
    t26g_fm0_wrong_scene_mapping.json  sha256 4f8f040e3ce566d1618f755f2dbdd77049f19a544ed3fe2298741a842a3aaa33
    splits/t26g_fm0_scene_folds.json  sha256 30ac0108d07efca19241d696ac6feb27fedb699d997017814f89f4d8a2bd2ff5
```

Frozen upstream code (reread and re-hash before use):

```text
    t26g_f1_models_draft.py  sha256 57e3c95c871c310d0826211851963f04d028b4a8788f79c8dba119746bf53abd
    t26g_f1_losses_draft.py  sha256 bcca490bd1d11a411ad4f4cae52c3428c28383150d10e172cd49c81a1f726476
    t26g_f1_conformance_draft.py  sha256 06439ca0e231d533ac43d690321ef13c00864c51e9ea5e0efbdf5e15bf5f038d
    t26g_d_models.py  sha256 919a462effb57117ae541932eff239112619959be4e24cd28a007227ed15cdf3
    t26g_d_losses.py  sha256 c3e4dc997aca05e5e9409e0791e4ae823f986696b477ef8911df8b0e0d6dd40d
    t26g_e_train.py  sha256 8f365ad1184abffd67c7de739af8f28cc0aea24d4ef5798b995c43d7917e2c6f
    t26g_e_topoflist.py  sha256 df54826f056033b5783d30c2335b2fc945115d9a68f422aa789c7000d8237166
    t26f_b1_models.py  sha256 81cf4fb82d18b95ae75f263e9bce5832e3a1e48ea66281d6853d446002443464
```

## 2. Arms and run population

Four arms, ONE nomination arm (P_FULL_L2_CONS):

```text
P_FULL_L2_CONS               H_g = full_L2 [3086,4096]
P_LASTTOKEN_L3_MATCHED_CONS  H_g = full_L2[-1:] (same FA2 capture)
P_NULL_SCENE_MATCHED_CONS    H_g = deterministic zero token [1,4096]
P_A_LEGACY_MATCHED           t26f_b1_models.SafeWorldV2('A'), historical
                             objective + checkpoint rule (T26G-B contract)
```

Matched trio: the frozen F0B SafeWorldF1 graph, 1,708,871 params per arm
(assert exact equality). Loader casts bf16 -> fp32 at batch assembly.
5 folds (sha256("T26G-FM|"+scene_id), sorted, mod 5; sizes 35/35/35/34/34),
seeds {0,1,2}: 4 x 5 x 3 = 60 runs, then the frozen refit rule.
Objective for the trio: the exact T26G-D consequence objective. Checkpoint
policy: the exact E1 CONSTRAINED_SELECTOR_FIRST archive implementation
(t26g_e_train.py), conformance counterexample asserted. The E1 selector
loss is NOT used (frozen decision; not revisitable).

## 3. Mandatory preflights (before ANY training step)

1. Authority verification of every hash in section 0 and 1.
2. F821 gate: `ruff check --select F821` clean on EVERY module in the FM1
   package (adapted modules included) before registration.
3. GPU budget gate (HARD): at FM0 time CUDA was unavailable (cuInit rc=3,
   kernel-level; NVML healthy). Re-measure on GPU: median/p95 K8 latency,
   peak incremental inference memory, fwd+bwd peak VRAM, against the
   frozen G9 ceilings (10 ms / 15 ms / 1 GiB / 2,000,000 params). REFUSE
   to train if the GPU is unavailable or any ceiling fails.
4. TRAIN_WORKERS preflight: benchmark {1,2,4} per the frozen
   training-and-compute contract; <= 40 synthetic-target optimizer steps
   per worker; eligibility requires 0 OOM, 0 numerical/hash failures,
   total CPU <= 16, safe headroom, and the determinism fixture (fold 0,
   seed 0, fixture definition frozen in the contract; FM0 reference state
   hash 6b8930b8314a29f2... on cpu) bit-identical solo vs
   under concurrent load ON THE SAME DEVICE. Selection: highest eligible
   level with >= 10% aggregate throughput gain over the previous eligible
   level, else the previous.
5. Synthetic conformance re-run (the frozen F0B suite) must PASS on the
   execution device.

## 4. Execution order

1. Train the 60 runs (outer-fold isolation; normalization from
   outer-training scenes only; inner-validation logs carry every
   mandatory checkpoint field).
2. Refits under the frozen refit-epoch rule.
3. Predictions: NORMAL for all arms over outer-test folds; ablations on
   the trained FULL models: WRONG_SCENE_FULL_L2 (frozen donor bijection),
   ZERO_FULL_L2, LAST_TOKEN_ONLY_DIAGNOSTIC. K=2/5 use the frozen
   candidate-index prefixes; never recompute a candidate's score under a
   different set.
4. PREDICTION LOCK: write + hash-register every prediction file, then
   STAGE_PREDICTIONS_LOCKED. No endpoint before the lock; analyzer hash
   registered before endpoints.
5. Endpoints + statistics per the frozen contract (10,000 paired scene
   bootstraps, seed 20260803, 95% CI; aggregation group -> scene -> seed
   -> paired scene -> mean over 173).
6. Apply the ten gates EXACTLY ONCE from the frozen contracts
   (t26g_fm0_promotion_gate_contract.json). No reinterpretation.

## 5. Record and payload separation

FM1 record root (system disk, MB-scale):
artifacts/safeworld_t26g_fm1_full_l2_matched_selector_experiment/<UTC>
No /storage payload tree is needed; model checkpoints and prediction npz
stay in the record (MB-scale). FA2 payloads are read-only.

## 6. Resume and failure behaviour

Every training run writes an atomic per-run completion record; re-running
the driver resumes from the last hash-verified run. Any defect repair
during execution follows the FA2 precedent: record a numbered mid-run
repair audit (defect, fix, module shas, scientific impact) and never
touch a finalized upstream record. Crash-looping or any frozen-contract
violation: STOP and finalize with the blocker.

## 7. Prohibitions

```text
NO access to the 35 confirmation scenes (T26G_FM_FROZEN_UNTOUCHED_
  CONFIRMATION_COHORT_V1) — IDs in the frozen contract; on PASS, FM1 may
  only GENERATE the FM2 confirmation preregistration, never touch them
NO sealed/protected cohort access (T26G-A 100+20, permanent sealed 2)
NO new scene download, Alpamayo generation, AlpaSim/NuRec/evaluator run
NO change to any frozen contract, margin, fold, seed or tie rule
NO endpoint before the prediction lock
NO post-hoc arm, loss or checkpoint variant
```

## 8. Terminal outcomes (exactly two)

```text
T26G_FM1_FULL_L2_PROMOTABLE_READY_FOR_FM2_CONFIRMATION
T26G_FM1_NO_FULL_L2_GAIN_CLOSE_LIGHTWEIGHT_READER_LINE
```

PASS requires all ten gates. On PASS, generate (do not execute) the FM2
confirmation preregistration for the 35 frozen scenes. On FAIL, the
lightweight full-L2 reader line CLOSES.

## 9. Execution-vehicle amendment (user-authorized, 2026-08-03)

This section was appended by the user's launch authority AFTER FM0
finalization. It changes HOW the frozen design executes — nothing else.
Every scientific step, its ordering, and every frozen constant remain
governed by sections 0–8 and the hash-pinned FM0 contracts. The
FM0-registered base prompt (sha256
6c99cc423830fb9f58e76a455b6d01b49f02dec94cb19e0606ff79133c78909a) remains
byte-identical inside the FM0 record; this launch copy appends only this
section, following the FA1A precedent of amending launch machinery without
touching finalized records. FM1's Phase 0 audit must record BOTH hashes
(the registered base and this launch copy) and verify that this copy is
byte-identical to the base through section 8.

**9.1 The interactive FM1 session SHALL:**

1. run the section 0/1 authority verification and ALL section 3
   preflights itself (they are bounded: hash checks, GPU gate,
   TRAIN_WORKERS benchmark, synthetic conformance — minutes, not hours);
2. build the COMPLETE unattended pipeline package under
   FM1_REC/code_artifacts/ and FM1_REC/scripts/:
   a resumable pipeline driver covering the whole of section 4 plus
   finalization, and four operator scripts —
   run_t26g_fm1_full.sh / watch_t26g_fm1_full.sh /
   stop_t26g_fm1_full.sh / resume_t26g_fm1_full.sh —
   modeled on the proven FA2 pattern: single-instance lock file,
   pipeline status log, heartbeat + progress + ETA files, per-run logs,
   atomic per-run completion records, hash-verified skip on resume,
   graceful stop, resume == rerun of the same script;
3. validate the package statically (F821 on every module, imports, dry
   argument construction, frozen-input loads) WITHOUT training;
4. return the exact tmux commands (session name `t26g_fm1_full`,
   absolute paths, tee to FM1_REC/logs/t26g_fm1_full_tmux.log);
5. STOP without launching. The user launches.

**9.2 The tmux-launched runner SHALL execute unattended, in order:**

```text
startup re-verification (quick hash checks, GPU gate, F821) — cheap
  insurance against state drift between session and launch; both the
  in-session preflight AND the runner's re-check must pass
60 training runs at TRAIN_WORKERS_FINAL (resumable)
refits under the frozen rule
predictions: NORMAL all arms + the three FULL ablations
PREDICTION LOCK (hash-register everything, STAGE_PREDICTIONS_LOCKED)
endpoints + statistics per the frozen contract
the ten gates, applied exactly once
on PASS: generate (never execute) the FM2 confirmation preregistration
write the terminal status, final manifest, reread QA and reports
```

Nothing in the analysis phase requires interaction, so the runner carries
FM1 to its terminal status even if the conversation that built it is long
closed. Heartbeat must be written at least once per training run and once
per analysis phase. A mid-run defect repair (section 6) may be applied
between a stop and a resume, with its numbered repair audit.

**9.3 Boundaries of this amendment.** It does NOT authorize: skipping or
reordering any section 3 preflight or section 4 step; weakening any gate;
training before the in-session preflights pass; auto-launching the tmux
session; or any deviation from the frozen contracts. If the package
cannot faithfully automate a step, the session STOPS with
BLOCKED_T26G_FM1_EXECUTION_VEHICLE_FAILURE rather than improvising.

**9.4 The session's terminal response must include:** the preflight
results (GPU gate numbers vs G9, TRAIN_WORKERS_FINAL with ladder
evidence, conformance PASS), the package file list with hashes, the
exact launch/watch/stop/resume commands, and confirmation that training
has NOT started.
